from time import sleep
import time
class Game():
    def __init__(self):
        time.sleep(2)
        self.greet = print('Welcome to RPSLS! Before we start, lets go over some of the rules.')
        time.sleep(1)
        self.rules = print('The rules are simple. \n Rock beats paper\n Paper loses to scissors \n Rock loses to paper\n')

    
